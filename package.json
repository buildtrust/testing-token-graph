{
  "name": "testing-token-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ buildtrust/testing-token-graph",
    "create-local": "graph create --node http://localhost:8020/ buildtrust/testing-token-graph",
    "remove-local": "graph remove --node http://localhost:8020/ buildtrust/testing-token-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 buildtrust/testing-token-graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
