type Proposal @entity {
  id: ID!
  txHashCommitted: Bytes!
  txHashAdmined: Bytes
  placeBlockNumber: String!
  status: Int! 
  proposer: Bytes! # address
  timeCommitted: BigInt!
  admin: Bytes # address
  timeAdmin: BigInt
}
